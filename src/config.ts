// Site configuration
// Edit these values to update site-wide settings

export const siteConfig = {
  // Set to true when open for collaboration, false when not
  openForCollaboration: false,
  
  // Collaboration message shown when open
  collaborationMessage: "Available for collaboration",
  
  // Optional: message shown when not available (set to null to hide the badge entirely)
  // unavailableMessage: null as string | null,
  // Example: unavailableMessage: "Currently unavailable",
  unavailableMessage: "Currently unavailable",
};
